# 磁盘清理指南

## 问题说明

您的 C 盘已满 (501GB/501GB = 100%)，导致无法安装或更新软件。

主要原因：
- **视频帧目录占用 23.9GB**: `sscisubagent-skills/SEOer/bilibili-automation/video_frames/`
- npm 缓存、临时文件等

## 清理步骤

### 方法 1: 自动清理（推荐）

#### Windows 用户:

1. **右键点击** `cleanup-disk-space.bat`
2. **选择"以管理员身份运行"**
3. 按照提示操作

或直接双击运行（可能无法清理某些需要权限的文件）

#### PowerShell 用户:

```powershell
# 以管理员身份运行 PowerShell
Set-ExecutionPolicy Bypass -Scope Process -Force
.\cleanup-disk-space.ps1
```

### 方法 2: 手动清理

如果自动脚本无法运行，请手动执行以下操作：

#### 1. 删除视频帧目录（最大清理项）

```powershell
# PowerShell
Remove-Item -Path "D:\stigmergy-CLI-Multi-Agents\sscisubagent-skills\SEOer\bilibili-automation\video_frames" -Recurse -Force
```

或使用文件资源管理器：
- 导航到 `D:\stigmergy-CLI-Multi-Agents\sscisubagent-skills\SEOer\bilibili-automation\`
- 删除 `video_frames` 文件夹

#### 2. 清理 npm 缓存

```bash
npm cache clean --force
```

#### 3. 清理 Windows 临时文件

- **Windows 10/11**:
  1. 按 `Win + I` 打开设置
  2. 进入 **系统** → **存储**
  3. 点击 **临时文件**
  4. 选择要删除的项目
  5. 点击 **删除文件**

#### 4. 清理项目 tarball 文件

```powershell
# PowerShell
Get-ChildItem -Path "D:\stigmergy-CLI-Multi-Agents" -Filter "*.tgz" -File | Remove-Item -Force
```

### 验证清理结果

```powershell
# 检查磁盘空间
Get-PSDrive C
```

应该看到至少有 **500MB** 的可用空间。

## 清理后的操作

### 重新安装 stigmergy

```powershell
# 在项目目录中运行
cd D:\stigmergy-CLI-Multi-Agents
npm install -g .

# 验证安装
stigmergy --version
stigmergy status
```

### 预期输出

```
1.3.54-beta.0

📊 CLI Tools Status:
  ✅ claude
  ✅ gemini
  ✅ qwen
  ...
```

## 故障排除

### 问题: "磁盘空间不足"

**解决方案**:
1. 运行 Windows 磁盘清理工具
2. 卸载不需要的程序
3. 清空回收站
4. 将大文件移到外部驱动器

### 问题: "权限不足"

**解决方案**:
1. 右键点击脚本 → "以管理员身份运行"
2. 或以管理员身份打开 PowerShell

### 问题: "stigmergy 命令找不到"

**解决方案**:
1. 确认 npm 全局路径在 PATH 中
2. 重新打开终端窗口
3. 使用完整路径: `C:\Users\YourName\AppData\Roaming\npm\stigmergy.cmd`

## 长期建议

1. **定期清理临时文件**
   - 每月运行一次磁盘清理

2. **监控磁盘空间**
   - 保持至少 10GB 的可用空间

3. **使用 .gitignore**
   - 已添加视频文件排除规则
   - 不会再次提交大文件

4. **考虑使用其他驱动器**
   - 将项目移到 D 盘或外部驱动器
   - 使用符号链接连接

## 联系支持

如果问题仍然存在：
- 检查错误日志
- 运行 `stigmergy diagnostic`
- 提交 issue 到项目仓库
