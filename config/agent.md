# Configuration Directory

Project configuration files:
- `superpowers/` - Soul evolution and automation configurations
- `builtin-skills.json` - Builtin skill definitions
- `gateway.json` - Gateway platform configurations
- `enhanced-cli-config.json` - Enhanced CLI settings
- `cc-connect-config.toml` - Claude Code connect settings

Environment-specific overrides can be placed here.
