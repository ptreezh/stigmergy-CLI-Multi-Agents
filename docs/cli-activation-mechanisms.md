# CLI Agents和Skills激活机制调查

## ❌ 我的诚实回答

### 问题1: 我知道哪些CLI需要激活吗？

**答案：❓ 我不知道，需要实际测试**

**原因**：
- 我只检查了配置文件和目录结构
- 我没有实际验证CLI是否真的使用这些agents/skills
- 我没有测试在CLI中调用agent或skill时会发生什么

### 问题2: 我知道如何激活吗？

**答案：❓ 我部分了解，但不确定**

**我看到的激活机制**：

#### 1. iFlow - Agent自动匹配
```
~/.iflow/IFLOW.md (配置文件)：
"在iFlow CLI中提及相关领域的任务，系统会自动选择合适的智能体。"

示例：
用户: "分析这个网络的中心性"
↓ (关键词匹配)
iflow识别为sna-expert的领域
↓
自动加载 sna-expert.md 的内容到上下文
```

#### 2. Qwen - 通过提示注入
```
~/.qwen/hooks.json：
{
  "qwen_superpowers": {
    "enabled": true,
    "context_injection": {
      "trigger_keywords": ["task", "project", "code", "debug", ...],
      "max_context_length": 2000
    }
  }
}

工作原理：
1. 用户输入包含trigger_keywords
2. Qwen注入相关的agents/skills上下文
3. 但不确定如何选择和注入哪些内容
```

#### 3. CodeBuddy/Copilot - Cross-CLI Adapter
```
~/.codebuddy/hooks.json：
{
  "cross_cli_adapter": {
    "trigger_patterns": [
      "use\\s+(\\w+)\\s+to\\s+(.+)$",
      "call\\s+(\\w+)\\s+(.+)$",
      "stigmergy\\s+(\\w+)\\s+(.+)$"
    ]
  }
}

工作原理：
1. 检测用户输入是否匹配trigger_patterns
2. 如果匹配，调用stigmergy命令
3. 例如：用户说 "use claude to write code"
4. → 调用 stigmergy claude "write code"
```

## 🔍 我不确定的事情

### ❓ 1. 部署后是否自动可用？

**iflow的agents**：
- ✅ 23个agent .md文件在 ~/.iflow/agents/
- ✅ README说"提及相关领域的任务，系统会自动选择"
- ❓ 但我不清楚：
  - 如何实现关键词匹配？
  - 是在prompt注入还是单独的Agent系统？
  - 是否需要在某个配置文件中声明？

**qwen的agents/skills**：
- ✅ 文件被复制到 ~/.qwen/agents/ 和 ~/.qwen/skills/
- ❓ 但我不知道：
  - qwen如何知道应该加载哪个agent？
  - hooks.json中的trigger_keywords是用于agent还是其他功能？
  - skills和agents的关系是什么？

### ❓ 2. 不同CLI的机制可能不同

| CLI | 可能的激活方式 | 确定度 |
|-----|---------------|--------|
| iflow | 关键词自动匹配agent | ⚠️ 部分了解 |
| qwen | trigger_keywords注入上下文 | ❓ 不确定 |
| codebuddy | trigger_patterns调用stigmergy | ✅ 了解 |
| copilot | trigger_patterns调用stigmergy | ✅ 了解 |
| claude | ❓ 未知 | ❌ 完全未知 |
| gemini | ❓ 未知 | ❌ 完全未知 |
| qodercli | ❓ 未知 | ❌ 完全未知 |

### ❓ 3. Agents vs Skills的区别

**iflow的agents**：
- 格式：独立的.md文件（ant-expert.md）
- 内容：专家描述、能力范围
- 激活：关键词匹配

**qwen的skills**：
- 格式：子目录+skill.md（brainstorming/skill.md）
- 内容：使用指南、工作流程
- 激活：❓ 不清楚

**问题**：
- agents和skills在同一个CLI中是什么关系？
- iflow只有agents，没有skills子目录吗？
- 还是iflow的agent就等同于qwen的skill？

## ⚠️ 我做的部署可能有问题

### 问题1: 部署了文件，但没有激活配置

**我做的**：
```javascript
// 只是复制文件
await fs.writeFile(targetPath, content, 'utf8');
```

**可能缺少的**：
```javascript
// 应该做的
// 1. 注册agents到CLI的配置
// 2. 更新hooks.json的trigger_keywords
// 3. 生成激活清单
// 4. 验证CLI能识别新部署的agents/skills
```

### 问题2: 机制假设可能错误

**我假设**：
- 所有CLI用类似的机制
- agents和skills是通用的概念

**实际情况**：
- ❓ 每个CLI可能有不同的agents/skills机制
- ❓ iflow的agent可能不等同于qwen的skill
- ❓ 需要分别研究每个CLI的实现

## 🔧 需要做什么

### 1. 验证部署是否生效

**测试方案**：
```bash
# 1. 检查部署前后的差异
ls ~/.qwen/agents/  # 部署前
# 部署
stigmergy config-deploy
ls ~/.qwen/agents/  # 部署后

# 2. 实际测试是否能调用
echo "测试ant-expert" | qwen "请用行动者网络理论分析这个网络"

# 3. 查看是否真的加载了ant-expert
```

### 2. 研究每个CLI的实际机制

**需要做的事情**：
- [ ] 阅读iflow源码，了解agent匹配逻辑
- [ ] 阅读qwen源码，了解skills加载机制
- [ ] 检查claude/gemini的agents/skills实现
- [ ] 对比不同CLI的机制差异

### 3. 修正部署逻辑

**可能的修正**：
```javascript
// 当前：盲目复制所有agents/skills
await this.deployConfigItem(cliName, 'agents', iflowAgents);

// 应该：只部署兼容的agents/skills
const compatibleAgents = filterCompatibleAgents(iflowAgents, targetCLI);
await this.deployConfigItem(cliName, 'agents', compatibleAgents);

// 并且：更新激活配置
await this.updateActivationConfig(cliName, compatibleAgents);
```

## 🎯 诚实的结论

### 我知道的：
✅ 文件被复制到正确的目录（agents/ 和 skills/）
✅ iflow通过关键词匹配自动选择agent
✅ codebuddy/copilot通过trigger_patterns调用stigmergy
✅ qwen有trigger_keywords机制（但细节不清楚）

### 我不知道的：
❓ 部署后的agents/skills是否真的被CLI识别和使用
❓ 是否需要额外的配置文件来激活
❓ 不同CLI的agents/skills机制是否兼容
❓ 如何验证部署是否生效

### 建议的下一步：
1. **实际测试**：在部署后尝试调用agent/skill，看是否生效
2. **源码研究**：阅读关键CLI的实现代码
3. **文档查找**：查找官方文档说明激活机制
4. **逐步验证**：一次部署一个CLI，验证后再扩展

---

**总结**：我之前过于自信地说"完成部署"，但实际上没有验证是否真的可用。需要你的帮助来确认实际效果。
