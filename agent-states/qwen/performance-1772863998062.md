[PATH] Detecting path for qwen...
[DETECTOR] Detecting path for qwen...
[DETECTOR] Found qwen in PATH: C:\Users\WIN10\AppData\Roaming\npm\qwen
[EXEC] qwen: Analyze Stigmergy performance and suggest optimizations.
基于对 Stigmergy CLI 系统的分析，我来提供性能分析和优化建议：

## Stigmergy 性能分析与优化建议

### 当前架构瓶颈

**1. 技能加载机制**
```javascript
// 当前实现：同步遍历 + 逐个 require
async loadSkills() {
  const skillDirs = fs.readdirSync(this.skillsDir, { withFileTypes: true })
    .filter(dirent => dirent.isDirectory())
    .map(dirent => dirent.name);

  for (const skillName of skillDirs) {
    const skill = await this.loadSkill(skillPath);  // 串行加载
  }
}
```
**问题**：串行加载技能，每个技能目录都要多次 `fs.readdirSync` 和 `require`

**2. 安全过滤性能**
```javascript
// 每次命令执行都要遍历多个正则
for (const pattern of this.blockedCommands) {
  if (pattern.test(command)) {
    return false;
  }
}
```

**3. 跨 CLI 通信超时**
- 默认超时 30 秒 (`timeout: 30`)
- 跨 CLI 通信超时 30 秒 (`communication_timeout: 30000`)
- 无请求缓存机制

**4. 技能发现缓存**
- `skill_cache_ttl: 300000` (5 分钟)
- 无增量更新，每次全量刷新

### 优化建议

| 优化项 | 当前状态 | 建议方案 | 预期收益 |
|--------|----------|----------|----------|
| **技能加载** | 串行同步 | 并行加载 + 懒加载 | 5-10x 启动速度 |
| **文件 I/O** | 同步 `readFileSync` | 异步 `readFile` + 批量读取 | 减少阻塞 |
| **安全过滤** | 实时正则匹配 | 预编译 + 白名单缓存 | 50%+ 过滤速度 |
| **技能缓存** | 全量刷新 | 基于文件 hash 增量更新 | 80%+ 缓存命中 |
| **跨 CLI 调用** | 无缓存 | 结果缓存 + 请求合并 | 减少重复调用 |

### 具体优化代码示例

**1. 并行技能加载**
```javascript
async loadSkills() {
  const skillDirs = await fs.promises.readdir(this.skillsDir, { withFileTypes: true });
  const skills = await Promise.all(
    skillDirs
      .filter(d => d.isDirectory())
      .map(async (dirent) => {
        const skillPath = path.join(this.skillsDir, dirent.name);
        return this.loadSkill(skillPath);
      })
  );
  skills.filter(Boolean).forEach(skill => {
    this.loadedSkills.set(skill.name, skill);
  });
}
```

**2. 增量技能缓存**
```javascript
async loadSkill(skillPath) {
  const skillMdPath = path.join(skillPath, 'SKILL.md');
  const stat = await fs.promises.stat(skillMdPath);
  const cached = this.skillCache.get(skillPath);
  
  if (cached && cached.mtime === stat.mtimeMs) {
    return cached.module; // 缓存命中
  }
  
  const module = await this._loadModule(skillPath);
  this.skillCache.set(skillPath, { module, mtime: stat.mtimeMs });
  return module;
}
```

**3. 预编译安全规则**
```javascript
constructor() {
  // 预编译正则，避免每次重建
  this.blockedPatterns = [
    /superpowers:.*[^a-zA-Z0-9_-]/i,
    /stigmergy.*superpowers:.*/i,
    // ...
  ].map(p => p);
  
  // 白名单 Set，O(1) 查找
  this.safeSuperpowersSet = new Set([
    'using-git-worktrees',
    'writing-plans',
    // ...
  ]);
}

isCommandSafe(command) {
  return !this.blockedPatterns.some(p => p.test(command)) &&
    this.validateSuperpower(command);
}
```

### 架构级优化建议

1. **引入技能注册表**：启动时生成技能索引，避免每次遍历文件系统
2. **消息队列**：跨 CLI 通信使用队列管理，支持优先级和重试
3. **连接池**：保持 CLI 进程池，避免频繁 spawn
4. **监控指标**：添加性能埋点，追踪技能加载时间、CLI 响应时间

需要我针对某个具体优化项提供详细实现方案吗？