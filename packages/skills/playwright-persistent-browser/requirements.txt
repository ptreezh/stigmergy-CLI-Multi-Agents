# Playwright 持久化浏览器 Skill 依赖文件
# 主要依赖 - Playwright 浏览器自动化框架
playwright>=1.40.0

# 可选依赖 - 用于高级功能
# rimraf>=5.0.0  # 用于清理会话目录

# 注意事项：
# 1. 安装后需要运行: npx playwright install chromium
# 2. Node.js版本需要 >= 16
# 3. 本项目使用ES modules，package.json中设置了 "type": "module"

# 安装命令：
# pip install -r requirements.txt  # 如果使用Python
# npm install  # 如果使用Node.js（推荐）

# 浏览器安装：
# npx playwright install chromium