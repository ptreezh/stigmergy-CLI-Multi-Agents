# 快速安装参考

> 🚀 Stigmergy-CLI 指定工具快速安装命令

## 📦 四个指定CLI工具

### 1. Claude Code
```bash
npm install -g @anthropic-ai/claude-code
```

### 2. Google Gemini CLI
```bash
npm install -g @google/gemini-cli
```

### 3. 通义千问 CLI
```bash
npm install -g @qwen-code/qwen-code
```

### 4. iFlow CLI (最新版本)
```bash
npm install -g @iflow-ai/iflow-cli@latest
```

## 🚀 一键安装所有工具

```bash
# 复制并运行以下命令
npm install -g @anthropic-ai/claude-code @google/gemini-cli @qwen-code/qwen-code @iflow-ai/iflow-cli@latest
```

## ✅ 验证安装

```bash
claude --version
gemini --version
qwen --version
iflow --version
```

## 🔧 部署扩展

安装完成后，运行：
```bash
cd deployment
node real-deploy.js
```

---

## 📋 其他可选的CLI工具

如果你想安装更多工具，可以参考：

### CodeBuddy CLI
```bash
npm install -g @codebuddy/cli
```

### QoderCLI
```bash
npm install -g qodercli
```

### GitHub Copilot CLI
```bash
npm install -g @github/gh-copilot
```