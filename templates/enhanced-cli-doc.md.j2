# {displayName} 协作指南

> 🚀 **Stigmergy协作增强** - 让您的{displayName}能够与其他AI CLI工具智能协作

## 📋 工具信息

- **工具名称**: {adapterName}
- **版本**: {version}
- **集成类型**: {integrationType}
- **配置文件**: {configFile}
- **部署时间**: {currentTime}

## 🤖 AI环境感知

当前项目已检测到以下可用的AI CLI工具:
{availableTools}

## 🔄 跨AI工具协作指南

### 中文协作指令
在{displayName}中，您可以使用以下格式调用其他AI工具：

```bash
# 基础调用格式
请用{tool}帮我{任务}

# 示例
请用qwen帮我生成一个Python函数
调用gemini来分析这段代码的性能
用iflow创建自动化测试工作流
```

### 英文协作指令
```bash
# 英文调用格式
use {tool} to {task}
call {tool} to {task}
ask {tool} for {task}

# 示例
use qwen to generate a Python function
call gemini to analyze code performance
ask iflow to create automated workflow
```

## 🛠️ 支持的协作工具

根据当前AI环境，您可以在{displayName}中调用以下工具：

| 工具 | 调用示例 | 专长领域 |
|------|----------|----------|
| **Claude CLI** | `请用claude帮我{task}` | 代码生成、架构设计 |
| **Gemini CLI** | `调用gemini来{task}` | 性能分析、代码优化 |
| **QwenCode CLI** | `用qwen帮我{task}` | Python开发、算法实现 |
| **iFlow CLI** | `用iflow帮我{task}` | 工作流自动化、CI/CD |
| **Qoder CLI** | `调用qoder来{task}` | 任务监控、进度管理 |
| **CodeBuddy CLI** | `调用codebuddy来{task}` | 代码审查、最佳实践 |
| **Copilot CLI** | `请用copilot帮我{task}` | 代码补全、智能建议 |
| **Codex CLI** | `用codex帮我{task}` | API生成、文档编写 |

## 💡 最佳实践

### 1. 任务分解策略
- 复杂任务可以分解为多个子任务，分配给不同的AI工具
- 利用每个工具的专长能力，提高整体效率

### 2. 协作工作流示例
```bash
# 1. 使用Claude进行架构设计
> 请用claude帮我设计这个系统的整体架构

# 2. 使用QwenCode实现核心功能
> 用qwen帮我实现这个架构的核心Python模块

# 3. 使用Gemini进行性能优化
> 调用gemini来分析优化这段Python代码的性能

# 4. 使用iFlow创建CI/CD
> 用iflow为这个项目创建完整的测试和部署工作流
```

### 3. 错误处理和恢复
- 如果某个工具调用失败，可以尝试使用其他工具完成相同任务
- 记录协作历史，便于后续优化工作流程

## 📊 项目记忆

此文档由Stigmergy CLI自动生成和维护，包含：
- 项目级AI协作配置
- 可用工具清单和能力映射
- 协作历史记录和最佳实践
- 动态更新的协作协议

## 🔄 更新和维护

```bash
# 重新扫描AI环境并更新配置
npx stigmergy-cli init --force

# 查看当前协作状态
npx stigmergy-cli status

# 更新所有工具的协作文档
npx stigmergy-cli update-all
```

---

**生成时间**: {currentTimeISO}  
**项目路径**: {projectPath}  
**仓库地址**: {repoUrl}  
**Stigmergy版本**: 1.0.0

> 🎉 **通过Stigmergy协作，让每个AI工具都发挥最大价值！**
